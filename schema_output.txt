Connected to Aiven!

Current columns:
  - id (int)
  - name (varchar(255))
  - email (varchar(255))
  - phone (varchar(50))
  - roll_no (varchar(50))
  - class (varchar(50))
  - year (varchar(10))
  - division (varchar(10))
  - class_name (varchar(100))
  - section (varchar(50))
  - department (varchar(100))
  - photo_folder_path (varchar(255))
  - face_encoding (mediumtext)
  - created_at (timestamp)

Applying missing column fixes...
  Γ£ö Already exists: email
  Γ£ö Already exists: phone
  Γ£ö Already exists: roll_no
  Γ£ö Already exists: class
  Γ£ö Already exists: year
  Γ£ö Already exists: division
  Γ£ö Already exists: class_name
  Γ£ö Already exists: section
  Γ£ö Already exists: department
  Γ£ö Already exists: photo_folder_path
  Γ£ö Already exists: face_encoding
  Γ£ö unique_roll_no key already exists

Final columns:
  - id (int)
  - name (varchar(255))
  - email (varchar(255))
  - phone (varchar(50))
  - roll_no (varchar(50))
  - class (varchar(50))
  - year (varchar(10))
  - division (varchar(10))
  - class_name (varchar(100))
  - section (varchar(50))
  - department (varchar(100))
  - photo_folder_path (varchar(255))
  - face_encoding (mediumtext)
  - created_at (timestamp)

Done!
